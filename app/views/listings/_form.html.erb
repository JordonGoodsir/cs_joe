

<%= simple_form_for(@listing) do |f| %> 
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

<% if @listing.errors.full_messages %> 
<%= p @listing.errors.full_messages %> 
<%end%>
<div class="container sell_card p-3 rounded"> 
<div class="row_align pb-3">
<h2>Sell</h2> 
</div>
  <div class="row row_align justify-content-center"> 
    <div class="col-sm">
<h4><%=Skin.find(params[:skin_id]).name%></h4>
</div>  
<div class="col-sm">  
<h4><%= Skin.find(params[:skin_id]).wear%></h4>  
</div>    

<div class="col-sm">  
<h4><%= Skin.find(params[:skin_id]).rarity%></h4>  
</div>
 </div>  
  
  <div class= "d-flex justify-content-center">
  <%= image_tag("http:\/\/cdn.steamcommunity.com\/economy\/image\/#{Skin.find(params[:skin_id]).icon_url}", class:"img-fluid img-thumbnail item_background no_border mt-3 w-75",style: "box-shadow: -1px 0px 19px 0px ##{Skin.find(params[:skin_id]).rarity_color.to_s};")%> 
  </div> 
    <h4 class="row_align mt-3">Average Price: $<%= Skin.find(params[:skin_id]).average_price%></h4>  
  
  <div class="form-inputs d-flex justify-content-center mt-3">
    <%= f.input :selling_price %> 
  </div> 
  <p id="notice"><%= notice %></p>
 <center>
  <div class="form-actions d-flex justify-content-center mt-3 sell w-25">
   <%= f.button :submit %>
  </div> 
  <center>   
<% end %>  

 
