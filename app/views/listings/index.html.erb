

  <%# <div class= "d-flex flex-column"> %>  
  <%= render "partials/user_banner"%>
  <p id="notice"><%= notice %></p>  


<div class="container-fluid"> 

<div class="jumbotron dash_jumbo m-1 mt-4">
   <div class="d-flex align-items-start justify-content-center">
  <h1 class="display-1 dash_header">Listings</h1>  
  </div> 
  </div>
  <div class="row row-cols-6 justify-content-around">
<% @listings.each do |v|%>  

<div class="row m-3 py-3 d-flex justify-content-center card"> 
<%=Skin.find(Listing.find(v.id).skin_id).name%>  
<br> 
<%= Skin.find(Listing.find(v.id).skin_id).wear%> 
<%= image_tag("http:\/\/cdn.steamcommunity.com\/economy\/image\/#{Skin.find(Listing.find(v.id).skin_id).icon_url}", class:"img-fluid img-thumbnail item_background my-2 no_border",style: "box-shadow: -1px 0px 10px -1px ##{Skin.find(Listing.find(v.id).skin_id).rarity_color.to_s};")%>
Average Price: $<%=Skin.find(Listing.find(v.id).skin_id).average_price%>  
<br> 
Your Price: $<%= v.selling_price %>   
 <div class="row">
 <div class="col "> 
     <%= link_to 'Edit', edit_listing_path(v.id,v.seller_id, v.skin_id), class:"sell mt-2" %>
    </div>  

    <div class="col "> 
      <%= link_to 'Destroy', v, method: :delete, data: { confirm: 'Are you sure?' }, class:"sell mt-2" %>
    </div>  
    </div> 

</div> 

<% end%>  
</div> 
 

