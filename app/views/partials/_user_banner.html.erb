<div class="jumbotron dash_jumbo">
   <div class="d-flex align-items-start"> 
   <%# finding the current users profile and checking to see if a picture is attatched %>
   <%if Profile.find(@current_profile).picture.attached?%>
   <%= image_tag(Profile.find(@current_profile).picture, class:"img-thumbnail mr-5")%> 
   <%end%>  
   <%# finding the current users profile and displaying their username %>
  <h1 class="display-1 dash_header"><%= Profile.find(@current_profile).username %></h1>  
  
  </div>
 
  <div class="d-flex justify-content-end add_funds_button">  
  <%# finding the current users profile, then finding their wallet  %>
   <div class="d-flex align-items-center mx-2"><h4>$<%= Profile.find(@current_profile).wallet %><h4></div> 
    <%= link_to "Add Funds", add_funds_path, class:"sell align-items-center px-2"%> 
  
  </div>
</div>  
<nav class="navbar navbar-expand-lg">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse justify-content-center " id="navbarNav">
    <ul class="navbar-nav dash_hover">
      <li class="nav-item active px-5">
        <a class="nav-link" href="#"><h3><%=link_to "Profile", profile_path(@current_profile)%></h3><span class="sr-only">(current)</span></a>
      </li> 
      <li class="nav-item active px-5">
        <a class="nav-link"><h3><%=link_to "Inventory", inventory_path(current_user)%></h3><span class="sr-only">(current)</span></a>
      </li> 
      <li class="nav-item active px-5">
        <a class="nav-link"><h3> <%=link_to "Listings", listings_path%></h3><span class="sr-only">(current)</span></a>
      </li>
    </ul>  
  </div>
</nav> 
<div class=orange_bar> </div>